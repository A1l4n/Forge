# Rust build output (DO NOT commit — gigabytes)
/target/
**/*.rs.bk
*.rlib
*.rmeta

# Note: Cargo.lock IS committed for binary crates (this one).

# Local SQLite databases — Luna's memory & sessions, never commit
*.db
*.db-journal
*.sqlite
*.sqlite3
forge.db
forge_test.db

# Environment / secrets — NEVER commit
.env
.env.local
.env.*.local
*.pem
*.key
secrets.toml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
logs/
*.log

# Local build helpers
build_with_vs.bat

# Backups
*.bak
*.tmp

# Stray nested scaffold (has its own .git) — leftover from initial setup
/Forge/
